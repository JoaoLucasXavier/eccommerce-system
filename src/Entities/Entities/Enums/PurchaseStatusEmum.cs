namespace Entities.Enums
{
    public enum PurchaseStatusEmum
    {
        InCart = 1, // no carrinho
        Purchased = 2, // comprado
        InPreparation = 3, // em preparação
        OnMyWay = 4, // a caminho
        delivered = 5, // entregue
        Received = 6, // recebido
        canceled = 7 // cancelado
    }
}
